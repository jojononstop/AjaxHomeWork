﻿@{
    ViewData["Title"] = "Register";
}

<h1>Register</h1>
<div class="row">
    <div class="col-3"></div>
    <div class="col-6">
        <form id="userForm">
            <div class="mb-3">
                <label for="userName" class="form-label">姓名：</label>
                <input type="text" class="form-control" id="userName" name="Name">
            </div>
            <div class="mb-3">
                <label for="UserEmail" class="form-label">電子郵件：</label>
                <input type="email" class="form-control" id="UserEmail" name="Email">
            </div>
            <div class="mb-3">
                <label for="userAge" class="form-label">年紀：</label>
                <input type="number" class="form-control" id="userAge" name="Age">
            </div>

            <div class="mb-3">
                <label for="userAvator" class="form-label">頭像：</label>
                <input type="file" class="custom-file-input" id="userAvator" name="avator">
            </div>

            <button id="button1" type="submit" class="btn btn-primary">送出</button>
        </form>
        <div class="alert alert-info" id="div1"></div>
    </div>
    <div class="col-3"></div>
</div>

@section Scripts {
    <script>
        const btn = document.querySelector('#button1');
        // const txtName = document.querySelector('#userName');
        // const txtAge = document.querySelector('#userEmail');
        // const txtEmail = document.querySelector('#userAge');
        const divInfo = document.querySelector('#div1');
		const theForm = document.querySelector('#userForm');

        btn.addEventListener('click', async (event) => {
            event.preventDefault(); // 防止頁面提交

        //透過FORMData物件讀取資料
			const formData = new FormData(theForm);
            //透過FORMData資料傳到Server
            const url = `@Url.Content("~/api/register")`;
            const response = await fetch(url, {
				method: 'POST',
				body: formData
            });
            const datas = await response.text();
            divInfo.innerHTML = datas;
            // for (const pair of formData.entries()) {
			// 	console.log(pair[0]);
			// 	console.log(pair[1]);
   //          }
            // 構建API請求的URL
            // const url = `@Url.Content("~/api/register")?name=${nameValue}&age=${ageValue}&email=${emailValue}`;

            // // 發送API請求
            // const response = await fetch(url);
            // const datas = await response.text();

            // 將API響應的內容插入到HTML中
            //divInfo.innerHTML = datas;
        });
    </script>
}
